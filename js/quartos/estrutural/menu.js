import { abrirMenu } from "./caixas.js"

$(document).on('click', '[class="card"]', function () {
    /*
    var passo = $(this)
    var passo2 = $(passo[0].children[0])
    var passo3 = $(passo2[0].children[1])
    var suite = passo3.text()
    let menu = document.forms.namedItem("menu").id
    document.getElementById(menu).innerHTML = ""
    $(`#${menu}`).append(
        `<button type="button" name="${suite}" id="btn_${suite}" class="btn manutencao">Manutenção</button>`+
        `<button type="button" name="${suite}" id="btn_${suite}" class="btn locado">Locação</button>`+
        `<button type="button" name="${suite}" id="btn_${suite}" class="btn faxina">Faxina</button>`+
        `<input type="button" id="bi_one" class="btn btn-warning acoes1" name="bt_${suite}" value="">`+
        `<input type="button" id="bi_two" class="btn btn-warning acoes2" name="bt_${suite}" value="">`+
        `<input type="button" id="bi_trh" class="btn btn-warning acoes3" name="bt_${suite}" value="">`
    )
    abrirMenu("modau-menu")*/
})
